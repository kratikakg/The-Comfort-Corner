import React from "react";

import { hero } from "../data";

const Hero = () => {
  return (
    <section>
      <div className="container mx-auto">Hero</div>
    </section>
  );
};

export default Hero;
